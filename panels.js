class Panels {
  constructor(setting) {
    this.root = document.createElement("div.panels")
    this.setting = setting
    document.body.appendChild(this.root)

  }
}

// slider -> 
// field -> "string"
// option -> [array]
// checkbox -> true/false
// button -> function()
// color -> ...
